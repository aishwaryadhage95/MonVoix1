<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ash.monvoix1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TexttoSpeech"
            android:label="@string/title_activity_textto_speech"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SpeechtoText"
            android:label="@string/title_activity_speechto_text"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Sending"></activity>
    </application>

</manifest>